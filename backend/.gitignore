# Environment variables (contains secrets!)
.env
.env.local
.env.production
.env.*.local

# Temporary deployment files
deployment.json

# Flutter dashboard build files (regenerated on each deploy)
public/dashboard/*
!public/dashboard/.gitkeep
